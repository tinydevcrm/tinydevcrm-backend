# Docker Compose file for taking local/production to aws-ecs/production for
# Amazon Web Services (AWS) Elastic Container Service (ECS).

# AWS ECS only supports major versions of Docker Compose, not minor versions.
version: '3.0'

services:
    web:
        build:
            context: ./app
            dockerfile: production.Dockerfile
        command: gunicorn wsgi:application --bind 0.0.0.0:8000
        volumes:
            - static_volume:/home/app/web/staticfiles
        expose:
            - 8000
        env_file:
            - ./app/conf/.env.prod
        depends_on:
            - db
    db:
        build:
            context: ./db
            dockerfile: Dockerfile
        volumes:
            - postgres_data:/var/lib/postgresql/data
        env_file:
            - ./db/conf/.env.prod
    nginx:
        build:
            context: ./nginx
            dockerfile: Dockerfile
        volumes:
            - static_volume:/home/app/web/staticfiles
        ports:
            - 1337:80
        depends_on:
            - web

volumes:
    postgres_data:
    static_volume:
